# services/user-service/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: msa-auth
  labels:
    app: msa-auth
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
